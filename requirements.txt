# === ThriftFlow — dependency inti ===
Flask==3.1.3
Flask-SQLAlchemy==3.1.1
Flask-Login==0.6.3
python-dotenv==1.0.1
# Pillow: kompres & perkecil foto produk/logo otomatis saat upload (hemat kuota + cepat)
Pillow>=10.4,<13

# === Web Push (opsional) ===
# Dipakai untuk mengirim notifikasi push ke HP/desktop.
# Aplikasi tetap jalan walau ini tidak terpasang (notifikasi in-app tetap aktif).
pywebpush==2.0.3

# === Produksi (opsional) ===
# gunicorn untuk menjalankan di server produksi:  gunicorn "app:create_app()"
gunicorn==23.0.0
# psycopg untuk PostgreSQL (dipakai bila DATABASE_URL di-set, mis. di Railway/Render)
psycopg[binary]==3.2.3
